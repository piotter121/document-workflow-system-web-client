import { NumberOfModifiedLinesPipe } from './number-of-modified-lines.pipe';

describe('NumberOfModifiedLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfModifiedLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
