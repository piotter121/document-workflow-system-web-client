import { NumberOfInsertedLinesPipe } from './number-of-inserted-lines.pipe';

describe('NumberOfInsertedLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfInsertedLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
